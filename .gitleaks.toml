[extend]
useDefault = true

[allowlist]
description = "Canonical public parameter manifest (no secrets by construction), synthetic test fixtures (high-entropy hex ids/hashes), and the anvil well-known test mnemonic"
paths = ['''params/caravan\.params\.json''', '''fixtures/.*\.json''']
regexes = ['''test test test test test test test test test test test junk''']
